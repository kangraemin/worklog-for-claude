# Worklog: worklog — 2026-03-02

---

## 10:44

### 요청사항
- ccusage 의존성 제거, 워크로그 작성 로직을 worklog-write.sh로 중앙화, git post-commit hook 기반으로 전환

### 작업 내용
- ccusage 제거하고 자체 token-cost.py로 JSONL 직접 파싱하도록 변경
- worklog-write.sh 신규 작성: 토큰 계산 / 파일 쓰기 / Notion 전송 / 스냅샷 갱신 통합
- git post-commit hook 추가로 커밋마다 자동 워크로그 작성
- commands/worklog.md를 worklog-write.sh 호출하는 얇은 래퍼로 변경
- session-end timing 옵션 제거

### 변경 파일
- `commands/worklog.md`: worklog-write.sh 호출 래퍼로 단순화
- `git-hooks/post-commit`: AI_WORKLOG_DIR 훅 위임 래퍼 신규
- `hooks/post-commit.sh`: 커밋 후 claude -p 요약 → worklog-write.sh 호출 신규
- `install.sh`: git hook 설치 섹션 추가, session-end 옵션 제거
- `rules/worklog-rules.md`: each-commit → post-commit hook 기반으로 문서 변경
- `scripts/token-cost.py`: JSONL 직접 파싱 신규
- `scripts/worklog-write.sh`: 워크로그 작성 핵심 스크립트 신규
- `scripts/duration.py`: 프로젝트 CWD 기반 필터링 수정
- `tests/test_install_e2e.py`: session-end 제거 반영, timing 입력 시퀀스 조정

---

## 10:57

### 요청사항
- Stop hook으로 자동 커밋 기능 추가, 설치 시 사용 여부 질문

### 작업 내용
- hooks/stop.sh 신규: 미커밋 변경사항 감지 → git-rules 우선순위 탐색 → claude -p 커밋
- rules/auto-commit-rules.md 신규: git-rules 없는 유저용 최소 커밋 규칙
- install.sh: each-commit 선택 시 자동 커밋 질문 추가, 조건부 Stop hook 등록

### 변경 파일
- `hooks/stop.sh`: Stop hook 자동 커밋 로직 신규
- `rules/auto-commit-rules.md`: 최소 커밋 규칙 신규
- `install.sh`: 자동 커밋 질문 + Stop hook 조건부 등록
- `tests/test_install_e2e.py`: 자동 커밋 입력 추가, Stop hook 등록 테스트

---

## 11:03

### 요청사항
- Notion 워크로그 타이틀이 "작업 내용"으로 잘못 추출되는 버그 수정

### 작업 내용
- worklog-write.sh 타이틀 추출 시 `### 헤더`를 스킵하도록 grep -v '^###' 추가
- 기존 잘못된 Notion 엔트리 3개 타이틀 수정, 부정확한 cost/tokens 초기화

### 변경 파일
- `scripts/worklog-write.sh`: 타이틀 추출에서 ### 헤더 스킵 추가
---

## 11:30 (auto)

### 작업 내용
- fix: post-commit hook cost 비정상 해결 — --no-cost 옵션 추가

### 변경 파일
- `hooks/post-commit.sh`
- `scripts/worklog-write.sh`

---

## 11:34 (auto)

### 작업 내용
- fix: post-commit 프롬프트에 파일별 설명 요구 + cost 유지

### 변경 파일
- `hooks/post-commit.sh`

---

## 12:18 (auto)

### 작업 내용
- docs: README를 실제 아키텍처에 맞게 갱신
- post-commit hook 기반 자동 워크로그, stop hook auto-commit,
- 아키텍처 다이어그램·스크립트·커맨드 섹션 추가

### 변경 파일
- `README.md`

---

## 13:20 (auto)

### 변경 파일
- `commands/finish.md`
- `hooks/post-commit.sh`
- `hooks/stop.sh`
- `install.sh`
- `tests/test_install_e2e.py`
- `tests/test_post_commit_e2e.py`

